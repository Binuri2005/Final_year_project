import 'package:flutter/material.dart';

class IndividualRolePlayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Individual Role-plays')),
      body: Center(child: Text('Welcome to Individual Role-plays!')),
    );
  }
}
